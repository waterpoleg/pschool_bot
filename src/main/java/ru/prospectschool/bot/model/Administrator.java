package ru.prospectschool.bot.model;

public class Administrator extends Empoyee {

    public Administrator(int id, String name, String position, String bio, byte[] photo) {
        super(id, name, position, bio, photo);
    }
}
